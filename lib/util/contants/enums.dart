//List of enums
//they cannot be creates in class.

enum TextSizes {small, medium, large}

enum OrderStatus { pending, shipped, delivered }

enum PaymentMethod { creditCard, masterCard, visa ,paypal, bankTransfer, easypaisa, jazzCash }
